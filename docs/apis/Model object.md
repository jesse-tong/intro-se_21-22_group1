# Model object and APIs

## Model structure

- In the files inside models directory in backend source directory, there are several classes with @dataclass decorator and inherits db.Model class (@ before a class denotes )